spring:
  datasource:
    url: jdbc:mysql://localhost:3309/delivery_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: testuser
    password: testpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # 테스트니까 auto-update
    show-sql: true
    properties:
      hibernate.format_sql: true
